<!-- views/userHome.ejs -->
<!doctype html>
<html>
<head>
    <title>TimeLog - User Home</title>
    <meta charset="UTF-8" name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="//cdn.datatables.net/1.10.16/css/jquery.dataTables.min.css">
    <link rel="stylesheet" type="text/css" href="//cdn.datatables.net/buttons/1.4.2/css/buttons.dataTables.min.css">
    <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <!--<link rel="stylesheet" href="//unpkg.com/flatpickr/dist/flatpickr.min.css">-->
    <script src="../3rdPartyLibs/jquery-3.3.1.min.js"></script>
    <script src="//cdn.datatables.net/1.10.16/js/jquery.dataTables.min.js"></script>
    <script src="//cdn.datatables.net/buttons/1.4.2/js/dataTables.buttons.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <script src="//cdn.jsdelivr.net/npm/flatpickr"></script>
    <!--<script src="//unpkg.com/flatpickr"></script>-->
    <style>
        body{
            padding-bottom: 80px;
            word-wrap:break-word;
        }
        .floatingBox{
            display: inline;
        }
        .floating-Box {
            display: inline-block;
            /*width:350px;*/
        }
        table {
            overflow-x: auto;
            width: 100%;
            display: block;
            margin-bottom: 20px;
            border-collapse: collapse;
        }

        table,
        th,
        td {
            border: 1px solid #cdcdcd;
            text-align: center;
        }

        table th,
        table td {
            padding: 10px;
            text-align: left;
        }

        table::-webkit-scrollbar-thumb {
            border-radius: 2px;
            background-color: rgba(0, 0, 0, .5);
            -webkit-box-shadow: 0 0 1px rgba(255, 255, 255, .5);
        }

        table::-webkit-scrollbar {
            -webkit-appearance: none;
            height: 7px;
        }
        .navul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            /*overflow: hidden;*/
        }
        .navbutton1 {
            width: 110px;
            font-size: 16px;
            height: 80px;
            margin: 0;
            border-radius: 0px 0 0;
            background-color: #000000;
            border: 0 solid;
            color: #ffffff;
            -webkit-transition-duration: 0.4s;
            transition-duration: 0.4s;
        }

        .navbutton1:hover {
            background-color: #333;
            color: white;
        }

        .dropdown {
            position: relative;
            display: inline-block;
        }
        .button1 {
            height: 40px;
            width: 110px;
            float: right;
            border-radius: 5px;
            -webkit-transition-duration: 0.4s;
            transition-duration: 0.4s;
            cursor: pointer;
            background-color: white;
            color: #337ab7;
            border: 1px solid #337ab7;
        }

        .button1:hover {
            background-color: #337ab7;
            color: white;
        }

        .dropbutton {
            float: right;
            width: 150px;
            height: 80px;
            padding: 16px;
            color: white;
            background-color: #337ab7;
            border: none;
            cursor: pointer;
            font-size: 16px;
            -webkit-transition-duration: 0.4s;
            transition-duration: 0.4s;
        }
        .dropbutton:hover, .dropbutton:focus {
            background-color: #2b6699;
        }
        .dropdown-content {
            display: none;
            background-color: #f1f1f1;
            min-width: 160px;
            overflow: auto;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            right: 0;
            z-index: 1;
        }
        .dropdown-content a {
            color: black;
            padding: 20px;
            text-decoration: none;
            display: block;
        }
        .dropdown a:hover {
            background-color: #ddd;
        }
        .show {
            display: block;
        }
        /*div.floating-Box_clearForm {     !* this will hide div with class div_class_name *!*/
        /*    display:none;*/
        /*}*/
        /*div.buttons {     !* this will hide div with class div_class_name *!*/
        /*    display:none;*/
        /*}*/
    </style>
</head>
<div class="navul" style="height:80px;background:#000000;">
    <a class="navul" style="background:#000000;" href="http://aworldbridge.com">
        <img class="banner" src="/images/NewWBBanner.jpg" style="width: 500px;height:76px;"/>
    </a>
<!--    <a href="/homepageLI" style="width:110px;">-->
<!--        <button class="navbutton1"> Homepage </button>-->
<!--    </a>-->
<!--    <a href="/newRequest" id= "Request" style="width:110px;">-->
<!--        <button class="navbutton1" style="width: 140px"> New Request </button>-->
<!--    </a>-->
<!--    <a href="/recovery" id ="Restore" style="width:50px;">-->
<!--        <button class="navbutton1"> Restore </button>-->
<!--    </a>-->
    <a href="/dataHistory" style="width:110px;">
        <button class="navbutton1" style="width: 160px"> Data History </button>
    </a>
    <a href="/rejected" style="width:110px;">
        <button class="navbutton1" style="width: 160px; display: none"> Rejected Requests </button>
    </a>
    <div class="dropdown" style="height: 80px; width: 200px; float: right; margin: 0;">
        <button onclick="dropDown()" class="dropbutton" id="Username">
            <%= firstName%>
            <span class="caret"></span>
        </button>
        <div id="userMenuAdmin" class="dropdown-content">
            <a href="/userProfile"> User Profile </a>
            <a href="/userManagement"> User Management </a>
            <a href="/signout"> Sign Out </a>
        </div>
        <div id="userMenuRegular" class="dropdown-content">
            <a href="/userProfile"> User Profile </a>
            <a href="/signout"> Sign Out </a>
        </div>
    </div>
</div>
<body ononline="onFunction()" onoffline="offFunction()">

<div class="container center">
    <div class="page-header text-center header">
        <h1><span class="fa fa-home"></span><strong> Time Log - User Home </strong></h1><h4><span class="fa fa-user"></span>
            <strong>Account Name</strong>: <%= user.username %></h4>
                <strong id = 'Left'>Minutes Still Needed</strong><br>
                <strong id = 'total'>Total Time Left</strong>

    </div>
</div>
<br>

<div class="container" style="width: 900px;">
    <div class="center floating-Box clearForm" style="width: 690px;">
        <h4 class="floating-Box" style="width: 220px;"><strong>Time Log:&nbsp;</strong></h4>
        <div style="width: 350px;">
            <button id = "timeIn" style="width: 160px"> Log Start Time &nbsp;</button>
            <button id = "timeOut" style="width: 160px"> Log End Time &nbsp;</button><br><br>
        </div>
    </div>
    <br>
    <br>
    </div>
    <br>
</div>
<script type='text/javascript'>
    let firstName = "<%= firstName %>";
    let lastName = "<%= lastName %>";
    let username = "<%= user.username %>";
    // console.log(username);
    let UserRole = "<%= user.userrole %>";
    let queryStr;
    let logInButton = document.getElementById('timeIn');
    let logOutButton = document.getElementById('timeOut');
    let logInTime;
    let logOutTime;
    let logInLength;
    let seconds;
    let minutes;
    let hours;
    let buttonIn = document.getElementById('timeIn');
    let buttonOut = document.getElementById('timeOut');
    let displayHour;
    let displayMinute;
    let displaySecond;
    let adminButton = document.getElementById('edit');
    let addButton = document.getElementById('add');
    let removeButton = document.getElementById('remove');



    $(document).ready(function() {

        $(function () {

            flatpickr(".date", {
                dateFormat: "Y-m-d",
                time_24hr: true
            });

            if (UserRole === "Regular") {
                document.getElementById('searchname').style.display = "none"; //disables username search for regular members
                queryStr = "Current_Status=Pending&UID=" + username;
            } else {
                queryStr = "Current_Status=Pending";
                adminButton.disabled = false;
            }
            console.log(queryStr);
            //queryAndDisplay(queryStr);

            //the console below refers to the above as querystr
            // just for the Pending table
            //By changing this we allow the initial table to only show pending records
            //We change to UID b/c of column name in Layer_Request form table

        });

        $.ajax({
            url: 'display',
            method: 'get',
            dataType: "json",
            success: function (results) {
                displayHour = Math.trunc(results/60);
                displayMinute = Math.trunc(results - (displayHour*60));
                displaySecond = Math.trunc((results - ((displayHour * 60) + displayMinute)) * 60);
                let display = "Time Still Needed: " + displayHour + ":" + displayMinute + ":" + displaySecond;
                document.getElementById('Left').innerHTML = display;
            }})

        $.ajax({
            url: 'displayTotal',
            method: 'get',
            dataType: "json",
            success: function (results) {
                displayHour = Math.trunc(results / 60);
                displayMinute = Math.trunc(results - (displayHour * 60));
                displaySecond = Math.trunc((results - ((displayHour * 60) + displayMinute)) * 60);
                let display = " Total Time Required: " + displayHour + ":" + displayMinute + ":" + displaySecond;
                document.getElementById('total').innerHTML = display;
            }
        })
    });

    function dropDown() {
        console.log(UserRole);
        if (UserRole === "Admin") {
            document.getElementById("userMenuAdmin").classList.toggle("show");
        } else if (UserRole === "Regular") {
            document.getElementById("userMenuRegular").classList.toggle("show");
        }
    }
    window.onclick = function(event) {
        if (!event.target.matches('.dropbutton')) {
            let dropdowns = document.getElementsByClassName("dropdown-content");
            for (let i = 0; i < dropdowns.length; i++) {
                let openDropdown = dropdowns[i];
                if (openDropdown.classList.contains('show')) {
                    openDropdown.classList.remove('show');
                }
            }
        }
    }

    function onFunction() {
        alert ("You are now online.");
    }

    function offFunction() {
        alert ("Your are offline. Some functionality may be unavailable.");
    }

    logInButton.addEventListener("click", function () {
        $(function () {
            flatpickr(".date", {
                enableTime: true,
                dateFormat: "Y-m-d",
                timeFormat: "H:i:S",
                time_24hr: true
            });
        });
        let today = new Date();
        let date = today.getFullYear() + '-' + (today.getMonth() + 1) + '-' + today.getDate();
        let time2 = today.getHours() + ":" + today.getMinutes() + ":" + today.getSeconds();
        //let dateTime = date + ' ' + time2;
        //logInTime = new Date();
        //let send = logInTime.getFullYear() + "-" + logInTime.getMonth() + "-" + logInTime.getDate();
        //let send1 = logInTime.getHours() + ":" + logInTime.getMinutes() + ":" + logInTime.getSeconds();
        //console.log(logInTime.getUTCMonth());
        //console.log(send1);
        //buttonOut.disabled = false;
        //buttonIn.disabled = true;
        $.ajax({
            url: 'inTime',
            data: {date, time2},
            method: 'post',
            dataType: 'json',
            success: function(results){
                //console.log(results);
                //console.log("boi WHY ARE YOU LOOKING AT THE CONSOLE for what reason are you here like u aren't editing this so why are u here?");
                alert("You have successfully clocked in!")
            }
        })
        //alert("You have now clocked in please do not close or refresh this tab make sure to click clock out when you leave")
    })

    logOutButton.addEventListener("click", function () {
        $(function () {
            flatpickr(".date", {
                enableTime: true,
                dateFormat: "Y-m-d",
                timeFormat: "H:i:S",
                time_24hr: true
            });
        });
        let today = new Date();
        let date = today.getFullYear() + '-' + (today.getMonth() + 1) + '-' + today.getDate();
        let time2 = today.getHours() + ":" + today.getMinutes() + ":" + today.getSeconds();
        //let dateTime = date + ' ' + time2;
        //logOutTime = new Date();
        // logInLength = logOutTime.getTime() - logInTime.getTime();
        // console.log(logInLength);
        // seconds = logInLength / 1000;
        // minutes = seconds / 60;
        // minutes = minutes.toString();
        // hours = minutes / 60;
        // console.log(hours);
        // hours = hours.toString();
        //let cool = logOutTime.getFullYear() + "-" + logOutTime.getMonth() + "-" + logOutTime.getDate();
        //let lmao = logOutTime.getHours() + ":" + logOutTime.getMinutes() + ":" + logOutTime.getSeconds();
        let ret;
        $.ajax({
            url: 'time',
            data: {date, time2},
            method: 'post',
            dataType: "json",
            success: function (results) {
                ret = results;
                //console.log(ret);
                alert(ret);
            }})
        //buttonOut.disabled = true;
        //buttonIn.disabled = false;
    })



</script>
</body>
</html>

